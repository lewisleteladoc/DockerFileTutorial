{
    "name": "my-app",
    "version": "1.0",
    "scripts": {
        "build": "echo 'No compilation step required for Express'",
        "test": "echo 'No tests defined yet' && exit 0",
        "galaxy": "node src/server.js"
    },
    "dependencies": {
        "express": "4.18.2"
    }
}
